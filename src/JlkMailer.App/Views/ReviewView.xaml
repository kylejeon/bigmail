<UserControl x:Class="JlkMailer.App.Views.ReviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="검토 · 정제" Style="{StaticResource H1}" />
        <TextBlock Grid.Row="0" Style="{StaticResource Caption}" HorizontalAlignment="Right" VerticalAlignment="Center"
                   Text="분류를 모르는 채 나가는 메일이 없도록, 문제 있는 행은 직접 처리해야 발송 대상이 됩니다." />

        <Border Grid.Row="1" Background="{StaticResource Surface2}" CornerRadius="4" Padding="12,8" Margin="0,6,0,10">
            <TextBlock Text="{Binding SummaryText}" Style="{StaticResource Mono}" />
        </Border>

        <!-- 필터 탭. SelectedItem 바인딩이면 커맨드 없이 SelectedFilter 가 갱신된다. -->
        <ListBox Grid.Row="2" ItemsSource="{Binding Filters}" SelectedItem="{Binding SelectedFilter}"
                 Margin="0,0,0,8" BorderThickness="0" Background="Transparent"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="0,0,6,6" />
                    <Setter Property="Padding" Value="10,5" />
                    <Setter Property="Cursor" Value="Hand" />
                    <Setter Property="FontFamily" Value="{StaticResource MonoFont}" />
                    <Setter Property="FontSize" Value="11.5" />
                    <Setter Property="BorderThickness" Value="1" />
                    <Setter Property="BorderBrush" Value="{StaticResource Rule}" />
                    <Setter Property="Background" Value="{StaticResource Surface}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Padding="{TemplateBinding Padding}" CornerRadius="4">
                                    <ContentPresenter />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{StaticResource Surface2}" />
                            <Setter Property="BorderBrush" Value="{StaticResource Navy}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Display}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <DataGrid Grid.Row="3" ItemsSource="{Binding VisibleRows}" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="False"
                  EnableRowVirtualization="True" EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  HorizontalGridLinesBrush="{StaticResource Rule}"
                  RowHeaderWidth="0" FontSize="12" Background="{StaticResource Surface}"
                  BorderBrush="{StaticResource Rule}" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="발송" Binding="{Binding Included, UpdateSourceTrigger=PropertyChanged}" Width="46" />
                <DataGridTextColumn Header="행" Binding="{Binding RowNo}" Width="52" IsReadOnly="True" />
                <DataGridTextColumn Header="병원 및 업체명" Binding="{Binding Hospital}" Width="200" IsReadOnly="True" />
                <DataGridTextColumn Header="성함" Binding="{Binding Name}" Width="72" IsReadOnly="True" />
                <DataGridTextColumn Header="진료과(원본)" Binding="{Binding DeptRaw}" Width="130" IsReadOnly="True" />
                <DataGridComboBoxColumn Header="세그먼트" Width="92"
                                        SelectedItemBinding="{Binding Segment, UpdateSourceTrigger=PropertyChanged}"
                                        ItemsSource="{Binding DataContext.Segments, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                <DataGridTextColumn Header="이메일" Binding="{Binding Email}" Width="220" IsReadOnly="True" />
                <DataGridCheckBoxColumn Header="교정" Binding="{Binding FixAccepted, UpdateSourceTrigger=PropertyChanged}" Width="46" />
                <DataGridTemplateColumn Header="상태" Width="86" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding StatusText}" FontWeight="SemiBold"
                                       Foreground="{Binding Severity, Converter={StaticResource SeverityToBrush}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="사유" Binding="{Binding Issue}" Width="*" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="교정 제안 일괄 승인" Command="{Binding AcceptAllFixesCommand}" Margin="0,0,8,0" />
            <Button Content="다시 분류" Command="{Binding ReclassifyCommand}" Margin="0,0,8,0" />
            <Button Content="보이는 행 제외" Command="{Binding ExcludeFilteredCommand}" Margin="0,0,8,0" />
            <Button Content="보이는 행 포함" Command="{Binding IncludeFilteredCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
